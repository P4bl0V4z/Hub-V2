import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>Frontend de Hub2.0 funcionando</h1>;

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<App />);